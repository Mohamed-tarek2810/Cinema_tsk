@model List<Categories>

<h2 style="font-size: 28px; margin-bottom: 20px;">Category List</h2>

<table style="width: 80%; border-collapse: collapse; margin: 20px auto; font-size: 18px;">
    <thead>
        <tr style="background-color: #f2f2f2;">
            <th style="padding: 12px; border: 1px solid #ddd;">ID</th>
            <th style="padding: 12px; border: 1px solid #ddd;">Name</th>
            <th style="padding: 12px; border: 1px solid #ddd;">Movies
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td style="padding: 10px; border: 1px solid #ddd; text-align: center;">@category.CategoriesId</td>
                <td style="padding: 10px; border: 1px solid #ddd;">@category.Name</td>
                <td style="padding: 10px; border: 1px solid #ddd;"><a href="Views/Home/@category.Name">Show Movies</a>
                @* <td style="padding: 10px; border: 1px solid #ddd;"> *@
                @*     @if (category.Movies != null && category.Movies.Any()) *@
                @*     { *@
                @*         <ul> *@
                @*             @for (int j = 0; j < category.Movies.Count; j++) *@
                @*             { *@
                @*                 var movie = category.Movies[j]; *@
                @*                 <li> *@
                @*                     <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.MoviesId"> *@
                @*                         @movie.Name ?? $"Movie #{movie.Id}" *@
                @*                     </a> *@
                @*                 </li> *@
                @*             } *@
                @*         </ul> *@
                @*     } *@
                @*     else *@
                @*     { *@
                @*         <span>No Movies</span> *@
                @*     } *@
                @* </td> *@
            </tr>
        <td>
            <a asp-action="Edit" asp-route-id="@category.CategoriesId">Edit</a> |
            <a asp-action="Details" asp-route-id="@category.CategoriesId">Details</a> |
            <a asp-action="Delete" asp-route-id="@category.CategoriesId">Delete</a>
        </td>
        }
    
    </tbody>
</table>
